# Spectra browser

This program is designed to visualize Raman spectra individually or in batches. With specific formatting of spectral names containing corrdinates, spectra can also be interpreted as 2D scans. 

## Installation

An anaconda envoronment in a `.yaml` format is supplied. This can be installed using the following command:

```bash
conda env create --name envname --file=environment.yml
```